pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Jenkins-pipeline'
            }
        }
        stage("docker") {
            steps {
                sh "ls"
            }
        }
    }
}